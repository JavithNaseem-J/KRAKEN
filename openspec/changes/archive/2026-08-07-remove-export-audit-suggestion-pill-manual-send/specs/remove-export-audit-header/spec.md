# remove-export-audit-header Specification

## ADDED Requirements

### Requirement: Header Bar Simplicity
The system MUST NOT render an Export Audit Log button in the main window header bar.

#### Scenario: Header rendering
- **WHEN** the chat header bar renders
- **THEN** no Export Audit Log button is present.
