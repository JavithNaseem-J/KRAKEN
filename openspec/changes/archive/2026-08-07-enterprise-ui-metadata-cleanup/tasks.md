## 1. Enterprise UI Metadata Cleanup

- [x] 1.1 Update `RuixenMoonChat.tsx` and `App.tsx` to render Session Title in the top header bar instead of raw session UUID
- [x] 1.2 Update `InlineApprovalCard.tsx` to truncate Approval ID to `#<short-hash>` with copy button and remove static protocol label
- [x] 1.3 Verify Vite build and test frontend UI rendering in browser
