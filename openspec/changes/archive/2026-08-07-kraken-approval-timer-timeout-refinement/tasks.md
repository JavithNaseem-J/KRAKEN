## 1. Pure Text Branding & Header Cleanup

- [x] 1.1 Update `SessionSidebar.tsx` header to render pure text `KRAKEN` without icon boxes
- [x] 1.2 Update top-right header polling text in `ruixen-moon-chat.tsx` to `Awaiting Security Authorization…`

## 2. Live Countdown Timer & Locked Expired State

- [x] 2.1 Refactor `InlineApprovalCard.tsx` header to minimal `Action Approval Gate` layout
- [x] 2.2 Add live 1-second countdown timer (`Expires in MM:SS`) to `InlineApprovalCard.tsx`
- [x] 2.3 Add locked expired state (`🔒 AUTHORIZATION EXPIRED`) to `InlineApprovalCard.tsx` and sync in `App.tsx`
- [x] 2.4 Verify Vite build and test frontend UI rendering in browser
